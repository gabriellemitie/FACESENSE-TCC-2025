<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.facesense.mobile">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="FaceSense Mobile"
        android:icon="@mipmap/ic_launcher">

        <!-- Inform Flutter tool that this app uses the v2 Android embedding -->
        <meta-data android:name="flutterEmbedding" android:value="2" />

        <!-- Use the new Android embedding (v2). MainActivity extends FlutterActivity -->
        <activity
            android:name="com.facesense.mobile.MainActivity"
            android:exported="true"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density"
            android:hardwareAccelerated="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
